PORT=8080
ENV=development

# SQLite Database (for agents)
DB_PATH=./data/monitoring.db

# Supabase (for env_metrics - optional)
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key
BOT_TELEGRAM_TOKEN=
# Format: single ID, comma-separated, atau JSON array
TELEGRAM_ID=

# Temperature Alert Thresholds (in Celsius)
# CPU temperature threshold (e.g., 80.0)
TEMP_ALERT_CPU_THRESHOLD=80.0
# MCU temperature threshold (e.g., 45.0)
TEMP_ALERT_MCU_THRESHOLD=45.0

# CORS Configuration (optional)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS,PATCH
CORS_ALLOWED_HEADERS=Content-Type,Authorization,Accept,Origin

# HTTPS Configuration (optional)
# Set to true to skip SSL certificate verification for self-signed certificates
HTTPS_INSECURE_SKIP_VERIFY=false